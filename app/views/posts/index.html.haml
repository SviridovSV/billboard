= link_to 'Create New', new_post_path
%br
- @posts.each do |post|
  = image_tag post.image.thumb.url unless post.image.blank?
  = truncate(post.text, :length => 160, separator: ' ')
  = post_author(post)
  = link_to 'Edit', edit_post_path(post)
  |
  = link_to "Delete", post_path(post), method: :delete, data: {confirm: "Really delete the post?"}
  |
  = link_to "Show", post_path(post)
  %br