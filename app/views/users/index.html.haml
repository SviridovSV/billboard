- @users.each do |user|
  .card{:style => "width: 18rem;"}
    .card-body
      %h5.card-title
        = user.login
      %p.card-text
        = user.email
      = link_to 'Profile', user_path(user), class: "btn btn-primary"