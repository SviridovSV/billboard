%h2 Add a comment:
= form_for([post, post.comments.build], remote: true) do |f|
  .field
    = f.label :body
    %br/
    = f.text_area :body
    %br/
    = f.hidden_field :user_id, value: current_user.id
  .actions
    = f.submit